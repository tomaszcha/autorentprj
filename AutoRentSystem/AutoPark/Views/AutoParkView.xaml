<UserControl x:Class="AutoPark.Views.AutoParkView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="600" d:DesignWidth="600">
    <UserControl.Resources>
        <DataTemplate x:Key="carParkTemplate">
            <Grid Width="500">
                <Grid.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Color="White" Offset="0"/>
                        <GradientStop Color="LightSteelBlue" Offset="1"/>
                    </LinearGradientBrush>
                </Grid.Background>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="350"/>
                    <ColumnDefinition Width="150"/>
                </Grid.ColumnDefinitions>
                <StackPanel Margin="10, 5, 0, 5" Grid.Column="0" VerticalAlignment="Stretch">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock FontSize="14" Text="{Binding Name}" />
                        <TextBlock FontSize="14" Margin="5,0,0,0" Text="{Binding Make.Name}" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock FontSize="12" Text="Seats:" />
                        <TextBlock FontSize="12" Margin="5,0,0,0" Text="{Binding Seats}" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock FontSize="12" Text="Day rate:"/>
                        <TextBlock FontSize="12" Margin="5,0,0,0" Text="{Binding DayRate}" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock FontSize="12" Text="Hour rate:"/>
                        <TextBlock FontSize="12" Margin="5,0,0,0" Text="{Binding HourRate}" />
                    </StackPanel>
                </StackPanel>
                <StackPanel Margin="10, 5, 0, 5" Grid.Column="1">
                    <Image Width="100" Height="100" Source="/AutoParkList;component/Images/Mazda.jpg" />
                </StackPanel>
            </Grid>
        </DataTemplate>
    </UserControl.Resources>
    <Grid x:Name="LayoutRoot" Background="White">
        <ListBox x:Name="myList" Width="500" Height="500" VerticalAlignment="Center" HorizontalAlignment="Center" 
                 ItemTemplate="{StaticResource carParkTemplate}" BorderThickness="1"/>
    </Grid>
</UserControl>
