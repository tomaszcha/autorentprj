<UserControl xmlns:sdk="http://schemas.microsoft.com/winfx/2006/xaml/presentation/sdk"  x:Class="ApplicationCreate.Views.ApplicationCreateView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="316" d:DesignWidth="500">
    <UserControl.Resources>
        <Style x:Key="SLabel" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="ExtraBlack"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Margin" Value="15,15,0,0"/>
        </Style>
        <Style x:Key="SComboBox" TargetType="ComboBox">
            <Setter Property="Margin" Value="0,10,30,5"/>
        </Style>
    </UserControl.Resources>
        <Grid x:Name="LayoutRoot" Opacity="0.5" Margin="5" Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="100*"/>
            <RowDefinition Height="10*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="350"/>
        </Grid.ColumnDefinitions>
        <Border BorderBrush="Black" BorderThickness="5" Grid.ColumnSpan="2" Grid.RowSpan="5"/>
        <TextBlock Grid.Column="0" Grid.Row="0" Text="Pickup: " Style="{StaticResource SLabel}"/>
        <ComboBox Grid.Column="1" Style="{StaticResource SComboBox}"/>
        <ComboBox Grid.Column="1" Grid.Row="1" Style="{StaticResource SComboBox}"/>
        <TextBlock Grid.Column="0" Grid.Row="2" Text="Return: " Style="{StaticResource SLabel}"/>
    </Grid>
</UserControl>
