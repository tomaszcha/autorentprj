<UserControl x:Class="SilverlightClientApp.Shell"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="clr-namespace:Microsoft.Practices.Prism.Regions;assembly=Microsoft.Practices.Prism"
    xmlns:navigation="clr-namespace:System.Windows.Controls;assembly=System.Windows.Controls.Navigation" 
    xmlns:uriMapper="clr-namespace:System.Windows.Navigation;assembly=System.Windows.Controls.Navigation"
    mc:Ignorable="d"
    d:DesignHeight="427" d:DesignWidth="801">
    
    <UserControl.Resources>
        <Style x:Key="MenuButton" TargetType="Button">
            <Setter Property="Background" Value="Red"/>
            <Setter Property="Width" Value="auto"/>
            <Setter Property="MinWidth" Value="150"/>
            <Setter Property="Margin" Value="3,0,3,0"/>
            <Setter Property="Opacity" Value="0.4"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontWeight" Value="ExtraBlack"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="100*"/>
            <RowDefinition Height="10*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Name="bttnHome" Style="{StaticResource MenuButton}" Content="Home"/>
            <Button Name="bttnAutoPark" Style="{StaticResource MenuButton}" Content="Auto park"/>
            <Button Name="bttnContract" Style="{StaticResource MenuButton}" Content="Conditions of rental"/>
            <Button Name="bttnAboutUs" Style="{StaticResource MenuButton}" Content="About us"/>
            <Button Name="bttnContacts" Style="{StaticResource MenuButton}" Content="Our contacts"/>
        </StackPanel>
        <ContentControl Grid.Column="0" Grid.Row="2" HorizontalAlignment="Stretch"
                       VerticalAlignment="Stretch"
                       prism:RegionManager.RegionName="LeftRegion"/>
        <ContentControl Grid.Column="1" Grid.Row="2" HorizontalAlignment="Stretch"
                       VerticalAlignment="Stretch"
                       prism:RegionManager.RegionName="RightRegion"/>
        <!--<Grid.Background>
            <LinearGradientBrush EndPoint="1,0.5" StartPoint="0,0.5">
                <GradientStop Color="Black" Offset="0" />
                <GradientStop Color="White" Offset="1" />
            </LinearGradientBrush>
        </Grid.Background>-->
        <Grid.Background>
            <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                <GradientStop Color="Black" Offset="0" />
                <GradientStop Color="#EC620505" Offset="0.488" />
            </LinearGradientBrush>
        </Grid.Background>
    </Grid>

</UserControl>
